# Pages

HTML entry points live in the **project root** for simple hosting:

| File | URL | Purpose |
|------|-----|---------|
| `../index.html` | `/` | Customer menu, cart, checkout |
| `../admin.html` | `/admin.html` | Admin dashboard |

Each page loads its own CSS and JS from `css/` and `js/`.
